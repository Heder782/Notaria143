// src/services/drive.js
export const uploadDocument = async (documentData) => {
  // Implementar autenticación OAuth2 y subida a Drive
  // Devolverá el ID del archivo y URL para QR
};

export const generateShareableLink = async (fileId) => {
  // Generar enlace público para el QR
};
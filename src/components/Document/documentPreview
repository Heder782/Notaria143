
import React from 'react';

const DocumentPreview = ({ documentData, qrCode, onEdit, onPrint, onSave }) => {
  return (
    <div className="document-preview">
      <div className="preview-controls">
        <button onClick={onEdit}>Editar</button>
        <button onClick={onPrint}>Imprimir</button>
        <button onClick={onSave}>Guardar en Drive</button>
      </div>

      <div className="document-template" id="document-to-print">
        <div className="document-header">
          <h1># {documentData.escritura}</h1>
          <h2>NOTARÍA PÚBLICA</h2>
          <h3>143</h3>
        </div>

        <div className="notaries-info">
          <p><strong>Lic. Carlos Alberto Díaz Hernández</strong><br/>Notario Público</p>
          <p><strong>Lic. Adelín Díaz García</strong><br/>Notario Público</p>
        </div>

        <div className="document-divider">---</div>

        <div className="document-body">
          <p>ESCRITURA NÚMERO {documentData.escritura}</p>
          <p>LIBRO NÚMERO {documentData.libro}</p>
          
          <p className="document-description">
            ES PRIMER TESTIMONIO DE LA ESCRITURA PÚBLICA QUE CONTIENE:<br/>
            LA <strong>{documentData.tipoActo.toUpperCase()}</strong>,<br/>
            {documentData.informacionActo}
          </p>
        </div>

        <div className="qr-section">
          <img src={qrCode} alt="Código QR" className="qr-code" />
          <p>Escanee este código para ver el documento completo</p>
        </div>

        <div className="document-footer">
          <div className="contact-info">
            <h4>CONTACTO</h4>
            <p>Teléfono: [Número]</p>
            <p>Email: [Email]</p>
            <p>Dirección: [Dirección]</p>
          </div>
          
          <div className="social-media">
            <h4>REDES SOCIALES</h4>
            <p>Facebook: /Notaria143</p>
            <p>Twitter: @Notaria143</p>
          </div>
        </div>
      </div>
    </div>
  );
};